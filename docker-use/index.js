const express = require('express')
const app = express()

apt.get('/',(req/res) => res.send('custom dockerfile for a simple express nodejs webapp'));

app.listen(3000, ()=>{
    console.log('Listening on port 3000')
})
